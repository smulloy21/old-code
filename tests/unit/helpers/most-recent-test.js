import { mostRecent } from '../../../helpers/most-recent';
import { module, test } from 'qunit';

module('Unit | Helper | most recent');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = mostRecent(42);
  assert.ok(result);
});
